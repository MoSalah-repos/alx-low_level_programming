#include <stdio.h>

/**
 * The function of this c code is to exactly print "Programming is like building a multilingual puzzle"
 * followed by a new line 
 * the program should return 0 after excusion
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
