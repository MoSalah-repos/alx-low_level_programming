#include "main.h"

/**
 * add -  adds two integers and returns the result.
 * @x: the first number
 * @y: the second number
 * Return: result of summation
 */

int add(int x, int y)
{
	return (x + y);
}
