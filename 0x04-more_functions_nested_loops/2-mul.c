#include "main.h"

/**
 *  mul - a function that multiplies two integers
 * @num1: defines the first number
 * @num2: sdefines the seecond number
 * Return: multiplication result of the two numbers
 */
int mul(int num1, int num2)
{
	return (num1 * num2);
}
