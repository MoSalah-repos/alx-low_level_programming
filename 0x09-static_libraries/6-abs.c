#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @num : the integer number to be tested
 * Return: absolute value of the num
 */
int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	else
	{
		return (-num);
	}
}
